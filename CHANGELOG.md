# Changelog

Detailed per-package history is generated by Changesets in `packages/ui/CHANGELOG.md`.

## Unreleased

- Initial project setup: pnpm workspace monorepo, tsup build, vitest tests, ESLint/Prettier, Changesets
- 11 MVP components: Button, IconButton, Card, Input, Badge, Spinner, Switch, Dialog, Toast, Tabs, Tooltip
- Design tokens (`--tori-*`), dark mode, ToriProvider
- CI (ci.yml) and npm Trusted Publishing workflows (publish.yml, publish-token.yml)
- Vite playground (apps/docs) and docs/ documentation
- Review hardening pass:
  - Supply chain: actions pinned to commit SHAs, `npm` environment gate on both
    publish workflows, release-tag ↔ package-version assertion, package-content verification
  - Performance: stable toast timers, memoized ToastCard, tooltip mounted only while open
  - API/type safety: dual ESM/CJS type exports, Slot composes handlers and refs,
    Button `asChild` parity (loading/icons/disabled), Switch `wrapperClassName`,
    `useControllableState` updater support
  - Accessibility: keyboard-accessible interactive Card, WCAG-checked gradient/badge
    foreground tokens, forced-colors focus outline, reduced-motion-safe spinner,
    stacked-dialog Escape/scroll-lock semantics, toast hover-pause
  - Tests: 17 → 59
  - All documentation and comments in English
